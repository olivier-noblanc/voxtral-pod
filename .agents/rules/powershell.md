---
trigger: always_on
---

tu es sur powershell (donc pas de grep,head etc.. mais plutôt | Select-Object -First 80 )
utilise un .js pour tes <script>
utilise un .css pour tes <style>
n'utilise pas de cdn télécharge les fichier nécessaire dans le projet
le projet risque de ne pas être maintenu souvent donc réfléchi bien avant de faire du js certaines fois c'est plus stable dans le temps de passer par une logique server side (comme le parsing markdow)